name: 'My First GitHub Actions Workflow'

on: push

jobs:
  first-job:
    name: 'First Job'

    runs-on: windows-latest

    steps:
    - name: Check secret
      shell: bash
      run: |
        echo ${{ secrets.SECRET_1 }}

    - name: Check secret
      shell: pwsh
      run: |
        if ("${{ secrets.SECRET_1 }}" -eq "OCA World") {
            echo "YES!"
        } else {
            echo "NO!"
        }