name: 'My First GitHub Actions Workflow'

on: push

jobs:
  first-job:
    name: 'First Job'

    strategy:
      matrix:
        os: [ 'windows-latest', 'macos-latest', 'ubuntu-latest' ]

    runs-on: ${{ matrix.os }}

    steps:
    - name: OCA power on ${{ matrix.os }}
      shell: bash
      run: |
        echo "OCA power on ${{ matrix.os }}"
